// This component is now empty since we're using CSS to handle the background
// We're keeping it for compatibility with the App.jsx structure
function ForestBackground() {
  console.log('ForestBackground: using CSS approach with body background');
  return null;
}

export default ForestBackground;